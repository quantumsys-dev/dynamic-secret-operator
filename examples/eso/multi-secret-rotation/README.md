# ESO + Multi-Secret Progressive Canary Rotation

This example demonstrates managing and rotating **multiple independent secrets** (PostgreSQL, Redis, and Payment API Gateway Key) bound to distinct volumes on a single workload with zero interference.

---

## Deploying

```bash
chmod +x deploy.sh
./deploy.sh
```
