apiVersion: v1
kind: Service
metadata:
  name: {{ include "dso.fullname" . }}-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dso.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "dso.selectorLabels" . | nindent 4 }}
