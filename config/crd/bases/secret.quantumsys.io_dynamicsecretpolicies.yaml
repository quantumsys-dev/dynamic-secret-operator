---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: dynamicsecretpolicies.secret.quantumsys.io
spec:
  group: secret.quantumsys.io
  names:
    kind: DynamicSecretPolicy
    listKind: DynamicSecretPolicyList
    plural: dynamicsecretpolicies
    singular: dynamicsecretpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.currentRevision
      name: Current Revision
      type: string
    - jsonPath: .status.desiredRevision
      name: Desired Revision
      type: string
    - jsonPath: .status.consecutiveFailures
      name: Failures
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DynamicSecretPolicy is the Schema for the dynamicsecretpolicies
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DynamicSecretPolicySpec defines the desired state of DynamicSecretPolicy.
            properties:
              canaryStrategy:
                description: CanaryStrategy configures progressive canary timing and
                  timeouts.
                properties:
                  timeoutSeconds:
                    description: TimeoutSeconds defines the maximum duration in seconds
                      to wait for canary health validation.
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - timeoutSeconds
                type: object
              rollbackConfig:
                description: RollbackConfig sets failure thresholds and rollback automation
                  behavior.
                properties:
                  autoRollback:
                    default: true
                    description: AutoRollback determines if the operator automatically
                      reverts to the last known good revision on failure.
                    type: boolean
                  circuitBreakerThreshold:
                    default: 3
                    description: CircuitBreakerThreshold is the maximum number of
                      consecutive failures permitted before halting reconciliations.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              validationProbes:
                description: ValidationProbes is a list of synthetic probes to run
                  against canary pods.
                items:
                  description: ValidationProbe configures synthetic probes executed
                    during rollout.
                  properties:
                    endpoint:
                      description: Endpoint is the network address or URL tested by
                        the probe.
                      type: string
                    queryTimeout:
                      description: QueryTimeout specifies probe timeout in seconds.
                      format: int32
                      minimum: 1
                      type: integer
                    type:
                      allOf:
                      - enum:
                        - TLS
                        - PostgreSQL
                        - MySQL
                        - HTTP
                      - enum:
                        - TLS
                        - PostgreSQL
                        - MySQL
                        - HTTP
                      description: Type of probe to execute (TLS, PostgreSQL, MySQL,
                        HTTP).
                      type: string
                  required:
                  - type
                  type: object
                type: array
              vaultRef:
                description: VaultRef references the external secret store and secret
                  identity.
                properties:
                  keyVaultURI:
                    description: KeyVaultURI is the URI of the Azure Key Vault or
                      secret backend.
                    minLength: 1
                    type: string
                  objectName:
                    description: ObjectName is the name of the secret, certificate,
                      or key within the vault.
                    minLength: 1
                    type: string
                  objectType:
                    allOf:
                    - enum:
                      - Secret
                      - Certificate
                      - Key
                    - enum:
                      - Secret
                      - Certificate
                      - Key
                    description: ObjectType specifies whether the secret is a Secret,
                      Certificate, or Key.
                    type: string
                required:
                - keyVaultURI
                - objectName
                - objectType
                type: object
              workloadSelector:
                description: WorkloadSelector identifies the target workload for progressive
                  rotation.
                properties:
                  kind:
                    description: Kind is the type of Kubernetes resource to target
                      (e.g. Deployment, StatefulSet).
                    minLength: 1
                    type: string
                  name:
                    description: Name is the name of the target workload resource
                      in the same namespace.
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - vaultRef
            - workloadSelector
            type: object
          status:
            description: DynamicSecretPolicyStatus defines the observed state of DynamicSecretPolicy.
            properties:
              conditions:
                description: Conditions represent the latest observations of an object's
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consecutiveFailures:
                description: ConsecutiveFailures tracks the number of consecutive
                  rollout/probe failures.
                format: int32
                type: integer
              currentRevision:
                description: CurrentRevision is the active, validated secret version
                  currently applied.
                type: string
              desiredRevision:
                description: DesiredRevision is the target secret version being rotated/canaried.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
